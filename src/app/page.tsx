import Image from "next/image";

export default function Home() {
  return (
    <main>
      {/* npx shadcn-ui@latest init && npx shadcn-ui@latest add && npm i @excalidraw/excalidraw react-draggable lucid-react axios uuid next-auth html2canvas function-plot jwt-decode next-auth-sanity/client @tabler/icons-react @react-oauth/google */}
    </main>
  );
}
